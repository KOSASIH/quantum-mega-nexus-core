def add_peer(host, port):
    """Add a new peer to the network."""
    # Here you would implement the logic to add a peer to the network
    print(f"Peer added: {host}:{port}")
    # You might want to update the peer list and notify other nodes
